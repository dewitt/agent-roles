# Claude Setup

## Startup
- Start Claude with instructions referencing `README.md`, `ROLES.md`, and `PROCESS.md`.
- Use the native runtime mode (one-shot or continuous).

## Tips
*   **Permissions**: Ensure `gh` and `git` operations are allowed.
*   **Context**: Read `PROCESS.md` first.
*   **Mechanics**: Coordinate through GitHub, not local files.